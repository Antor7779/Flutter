import 'package:flutter/material.dart';

void main() => runApp(
  MaterialApp(
    home: Scaffold(
      appBar: AppBar(
        title: Text('Customize and learn'),
        centerTitle: true,
        backgroundColor: Colors.red[600],
      ),
      body: Center(
        child: Image.asset('assets/cloth.jpg'),
        //  image: NetworkImage ('https://jubidubi.org/wp-content/uploads/2024/02/jUBIDUBI-300x167.png'),
            // image: AssetImage('assets/cloth.jpg'),
      //),
      ),
      floatingActionButton: FloatingActionButton(

        child: Text('Click'),
        backgroundColor: Colors.orange[600],
        onPressed: () {
          //  button press logic here
          print('Button Clicked');
        },
      ),
    ),
  ),
);

class test extends StatelessWidget {
  const test({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
//stateless .. stateful
